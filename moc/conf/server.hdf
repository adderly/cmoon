Config {
    logfile = /var/log/moon/moc
    trace_level = 7
}

Server {
    ip = 127.0.0.1
    port = 5000
    plugins {
        0 = skeleton
    }
}

Plugin {
    skeleton {
        dbsn = pgsql:dbname=merry host=localhost user=dida password=loveu
    }
}
